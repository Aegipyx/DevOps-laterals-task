services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: rust_server
    volumes:
      - db_data:/var/lib/postgresql/data

  backend:
    image: aegipyx/login-app-backend:latest
    restart: on-failure
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://user:password@db:5432/rust_server
    expose:
      - "8080"

  frontend:
    image: aegipyx/login-app-frontend:latest
    expose:
      - "80"
    depends_on:
      - backend
  
  nginx:
    build: ./nginx
    ports:
      - "3000:80"
    depends_on:
      - frontend
      - backend

volumes:
  db_data:
